-- Historical migration marker.
-- This migration was already applied to the production Netlify Database
-- by the previous Personal Pro Manager / CoachFlow deployment.
-- It must remain in the migrations history so Netlify can safely apply
-- subsequent migrations without treating the applied version as removed.
SELECT 1;
